torch>=2.0
matplotlib>=3.7
scikit-image>=0.21
pyvista>=0.42
numpy>=1.24
pytest>=7.0
pytest-cov>=4.0
jupyterlab>=4.0
nbstripout>=0.6

# Geometry import (issue #138) - reading uploaded meshes
trimesh>=4.0
# Native CAD (STEP/STP) import via OpenCASCADE (issue #161)
cascadio>=0.0.17

# Desktop application (issue #157) - the product UI, `python -m desktop`
PySide6>=6.6
pyvistaqt>=0.11

# Developer viewer app (issue #172) - app/main.py. A test harness for the
# geometry pipeline, not the product; retire once the desktop app covers it.
streamlit>=1.30
stpyvista>=0.1

# Only needed to regenerate the test meshes in
# examples/make_test_meshes.py (boolean backend for the finocyl slots).
manifold3d>=2.3
